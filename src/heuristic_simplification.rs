/// Remove internal nodes that are not contributing to branching structure
pub fn remove_internal(graph: &mut OverlapGraph, max_extension_len: usize,) {
    
    if max_bubble_len == 0 {
        return;
    }
}

// maybe start with a simpler function